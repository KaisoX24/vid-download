[project]
name = "vid-download"
version = "0.1.1"
description = "Add your description here"
authors = [
    { name = "KaisoX24", email = "acharjyapramit@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "typer>=0.27.1",
    "yt-dlp>=2026.8.19",
]

[project.scripts]
vid-download = "vid_download:main:app"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
